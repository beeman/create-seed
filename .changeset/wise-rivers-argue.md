---
"create-seed": minor
---

Run Biome formatting after dependency installation in scaffolding, so template config imports (like `tsdown.config.ts`) resolve correctly and scaffold output stays clean.
