---
'create-seed': minor
---

Rename template references across all files after scaffolding. When a template's package name differs from the user's chosen project name, all text files are searched and updated to use the new name.
